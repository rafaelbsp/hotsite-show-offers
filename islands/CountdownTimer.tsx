import Component from "$store/components/offerShow/CountdownTimer.tsx";
import type { Props } from "$store/components/offerShow/CountdownTimer.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
